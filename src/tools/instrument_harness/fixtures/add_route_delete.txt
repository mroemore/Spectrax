# add_route_delete.txt
# Task 4: verify the unified mod-sources container + ADD/REMOVE flow.
# The new layout is: header row (MODS label + ADD button) at item 0 of mod_wrap,
# followed by one row per mod-source entry. Core entries are attack/curve/decay/curve
# only; non-core entries also carry a type tag (ENV/LFO/RND), a stub ROUTE button,
# and a DEL button. The ROUTE button has no wiring yet (Tasks 5-7 will add it).
# This fixture exercises the full ADD -> walk-right -> REMOVE flow.

FRAMES 1
ASSERT selected==RATIO1
ASSERT envcount==4

# Move down to the mod-wrap header.
DOWN
DOWN
ASSERT selected==MODS_ADD

# EDIT fires cbAddModSource: appends a runtime env, rebuilds the graph,
# cursor lands on RATIO1.
EDIT
ASSERT envcount==5

# Re-navigate to the new runtime entry's ENV type button. From RATIO1,
# 7 DOWNs reach the new entry's ENV action button: RATIO3 -> MODS_ADD
# -> ATTACK(env[0]) -> ATTACK(env[1]) -> ATTACK(env[2]) -> ATTACK(env[3])
# -> ENV(env[4]).
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV

# EDIT cycles ENV -> LFO.
EDIT
ASSERT envcount==5
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==LFO

# Walk RIGHT through the entry to the DEL button.
# Layout: TYPE (3) | ATTACK CURVE DECAY CURVE (16) | ROUTE (3) | DEL (2)
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==DEL

# REMOVE removes the runtime source. After REMOVE, envcount drops to 4.
REMOVE
ASSERT envcount==4

QUIT
