# add_route_delete.txt
# Task 4: verify the unified mod-sources container + ADD/REMOVE flow.
# Task 5 (extension): verify the DEL button's YES/NO confirm modal —
#   EDIT on DEL pushes a layer with DELETE_NO pre-selected;
#   EDIT on DELETE_NO pops the layer (no source removed);
#   EDIT on DELETE_YES pops + calls removeSource.
# The ROUTE button has no wiring yet (Tasks 6-7 will add it).
# This fixture exercises the full ADD -> walk-right -> REMOVE flow,
# plus the DELETE_NO cancel path and the DELETE_YES confirm path.

FRAMES 1
ASSERT selected==RATIO1
ASSERT envcount==4

# Move down to the mod-wrap header.
DOWN
DOWN
ASSERT selected==MODS_ADD

# EDIT fires cbAddModSource: appends a runtime env, rebuilds the graph,
# cursor lands on RATIO1.
EDIT
ASSERT envcount==5

# Re-navigate to the new runtime entry's ENV type button. From RATIO1,
# 7 DOWNs reach the new entry's ENV action button: RATIO3 -> MODS_ADD
# -> ATTACK(env[0]) -> ATTACK(env[1]) -> ATTACK(env[2]) -> ATTACK(env[3])
# -> ENV(env[4]).
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV

# Task 6: ROUTE opens the destination-picking layer. The first collected
# dial is the meta-row VOICES dial (walk order: META precedes FM section).
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==ROUTE
EDIT
ASSERT topLayerSelected==voiceCount
# DOWN -> the first FM dial (op0 ratio).
DOWN
ASSERT topLayerSelected==ratio
# EDIT routes the runtime source to op0 ratio.
EDIT
ASSERT modulators==(0,1,0)
# cbRouteToDest pops + rebuilds, resetting selection to RATIO1.
# Re-navigate to ROUTE.
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==ROUTE
EDIT
ASSERT topLayerSelected==voiceCount
DOWN
ASSERT topLayerSelected==ratio
# EDIT un-routes.
EDIT
ASSERT modulators==(0,0,0)
# Re-navigate back to the runtime entry's ENV type button for the
# type-cycle + delete sections below.
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV

# EDIT cycles ENV -> LFO.
EDIT
ASSERT envcount==5
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==LFO

# Walk RIGHT through the entry to the DEL button.
# Layout: TYPE (3) | ATTACK CURVE DECAY CURVE (16) | ROUTE (3) | DEL (2)
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==DEL

# Task 5: DELETE_NO cancel path. EDIT fires cbDeleteSource which pushes
# a confirm layer with DELETE_NO pre-selected via changeGraphSelection.
EDIT
ASSERT topLayerSelected==DELETE_NO

# EDIT on DELETE_NO fires cbDeleteCancel (pops layer, no source removed).
# envcount stays at 5 — the runtime entry survives the cancelled delete.
EDIT
ASSERT envcount==5

# The pop left the base graph's selection unchanged (still on DEL).
# EDIT again opens the confirm layer for the same runtime entry.
EDIT
ASSERT topLayerSelected==DELETE_NO

# RIGHT selects DELETE_YES.
RIGHT
ASSERT topLayerSelected==DELETE_YES

# EDIT on DELETE_YES fires cbDeleteConfirmYes which pops the layer THEN
# calls removeSource(inst, idx), dropping envcount back to 4.
EDIT
ASSERT envcount==4

QUIT
