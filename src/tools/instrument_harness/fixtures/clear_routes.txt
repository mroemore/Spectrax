# clear_routes.txt - T12: KM_FUNCTION + KM_EDIT on a source's ROUTE button
# pushes the clear-all confirm (NO default). EDIT on NO cancels and leaves
# the base graph on the ROUTE button; RIGHT to YES + EDIT clears all of the
# source's routes. N1 (user report): the confirm's YES reselects the
# source's ROUTE button after its rebuild (so the cursor does not jump to
# the top of the screen). The route-removal semantics are asserted at the
# unit level (test_modsystem's removeModulationsForSource tests); this
# fixture covers the UI flow.
# FM instrument: boot selection = RATIO1; bank = fm1..fm5.

# --- ADD a runtime source so it has a ROUTE button ------------------------
DOWN
DOWN
ASSERT selected==MODS_ADD
EDIT
ASSERT envcount==5

# --- Navigate to the runtime source's ROUTE (RIGHT x5 from ENV type) -----
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==ROUTE

# --- Route op1 feedback (N2 lands the picker on es_Curve; JUMPTL to fb) --
EDIT
ASSERT topLayerSelected==es_Curve
UP
UP
UP
UP
ASSERT topLayerSelected==feedback
EDIT
# N1: the route's rebuild reselects the source's ROUTE button.

# --- KM_FUNCTION + KM_EDIT opens the clear-all confirm (NO default) -------
HOLD FUNCTION
EDIT
ASSERT topLayerSelected==CLEARALL_NO

# --- EDIT again on NO cancels: confirm closes, base stays on ROUTE --------
EDIT
RELEASE
ASSERT selected==ROUTE

# --- Re-open and confirm YES clears; N1 reselects the source's ROUTE ------
HOLD FUNCTION
EDIT
ASSERT topLayerSelected==CLEARALL_NO
RIGHT
ASSERT topLayerSelected==CLEARALL_YES
EDIT
RELEASE
ASSERT selected==ROUTE
