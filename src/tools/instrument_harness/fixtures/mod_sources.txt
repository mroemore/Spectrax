# mod_sources.txt - unified mod-sources container end-to-end
# FM instrument: core 4 envelopes, then runtime sources (env[4]...).
# Boot selection = RATIO1; bank = fm1..fm5.

# --- ADD a runtime source -------------------------------------------------
# DOWN DOWN lands on MODS_ADD (mod-wrap header); EDIT fires cbAddModSource.
DOWN
DOWN
ASSERT selected==MODS_ADD
EDIT
ASSERT envcount==5

# --- 3-way type cycle on the new source (ENV -> LFO -> RND -> ENV) --------
# After ADD (line 6) selection reset to RATIO1; DOWN x7 reaches env[4]'s
# TYPE button: RATIO3 -> MODS_ADD -> ATTACK(env0) -> ATTACK(env1) ->
# ATTACK(env2) -> ATTACK(env3) -> ENV(env4). Each EDIT cycles the type
# and rebuilds, resetting selection to RATIO1, so we re-navigate each time.
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV
EDIT
ASSERT envcount==5
# Rebuild reset selection to RATIO1; re-navigate to the type button (now LFO).
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==LFO
EDIT
# Now RND.
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==RND
EDIT
# Back to ENV.
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV

# --- Route the source to op0 feedback via the picking layer ----------------
# RIGHT x5 from ENV reaches ROUTE. EDIT opens the picker; N2 lands the
# cursor on the CLOSEST dest (env[4]'s own decay-curve dial, es_Curve).
# JUMPTL reaches the first "feedback" dest (op0) directly.
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==ROUTE
EDIT
ASSERT topLayerSelected==es_Curve
JUMPTL feedback
ASSERT topLayerSelected==feedback
EDIT
ASSERT modulators==(0,0,1)
# N1: cbRouteToDest pops + rebuilds and reselects env[4]'s ROUTE button —
# the cursor is already on ROUTE for the unroute.
EDIT
JUMPTL feedback
ASSERT topLayerSelected==feedback
# EDIT on a routed dest defers its toggle (T11 double-tap window) —
# wait for the deferred remove to fire.
EDIT
FRAMES 30
ASSERT modulators==(0,0,0)

# --- Delete via the confirm modal -----------------------------------------
# After the unroute N1 reselects env[4]'s ROUTE; RIGHT lands on DEL.
RIGHT
ASSERT selected==DEL
EDIT
ASSERT topLayerSelected==DELETE_NO
# EDIT on NO cancels: envcount stays 5.
EDIT
ASSERT envcount==5
# Re-open + confirm YES: envcount drops to 4 (core only).
EDIT
ASSERT topLayerSelected==DELETE_NO
RIGHT
ASSERT topLayerSelected==DELETE_YES
EDIT
ASSERT envcount==4

# --- Task 3.1: no-dial dests join the picker ------------------------------
# Re-add a runtime source; open its picker; the `gain` dest (unique name,
# no dial in the FM section) must be listed and routable.
DOWN
DOWN
EDIT
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
DOWN
ASSERT selected==ENV
RIGHT
RIGHT
RIGHT
RIGHT
RIGHT
ASSERT selected==ROUTE
EDIT
ASSERT topLayerSelected==es_Curve
JUMPTL gain
ASSERT topLayerSelected==gain
EDIT
ASSERT selected==ROUTE

QUIT
